<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:icon="@drawable/ic_baseline_menu_24"
    app:showAsAction="always"
    >

    <item android:id="@+id/liveCam"
        android:title="Live camera"
        app:showAsAction="never"/>

    <item android:id="@+id/images"
        android:title="Images"
        app:showAsAction="never"/>

    <item android:id="@+id/deleteAccount"
        android:title="Delete account"
        app:showAsAction="never"/>

    <item android:id="@+id/logOutMenu"
        android:title="Log out"
        app:showAsAction="never"/>

    <item android:id="@+id/addMenu"
        android:title="Tarantulas"
        android:icon="@drawable/ic_baseline_add"
        app:showAsAction="always"/>

</menu>